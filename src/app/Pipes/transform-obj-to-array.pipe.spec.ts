import { TransformObjToArrayPipe } from './transform-obj-to-array.pipe';

describe('TransformObjToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformObjToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
